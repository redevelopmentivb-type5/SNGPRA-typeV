services:
  - type: web
    name: sngpra-type-v-complaint-portal
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: ADMIN_EMAIL
        value: redevelopmentivb@gmail.com
      - key: ADMIN_PASSWORD
        sync: false
